apiVersion: v1
kind: Service
metadata:
  name: orchestrator-service
  labels:
    app: orchestrator-service
spec:
  selector:
    app: orchestrator-service
  ports:
    - port: 80
      targetPort: 8080
      name: http